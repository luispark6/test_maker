import { Calendar } from "lucide-react";

import Section from "../components/Section";

const DDayPrep = () => (
    
  <Section
    title="D-Day Prep"
    description="Structured 1-3 day study plan optimized for upcoming exams. Includes practice tests and review sessions."
    icon={<Calendar className="w-8 h-8" />}
  />
);

export default DDayPrep;
